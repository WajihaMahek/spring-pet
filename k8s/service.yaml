apiVersion: v1
kind: Service
metadata:
  name: springboot-app-lb
  labels:
    app: springboot-app
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: springboot-app
